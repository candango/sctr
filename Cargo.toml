[package]
name = "sctr"
version = "0.0.1"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0"
description = "A policy-controlled systemd lifecycle service for tenant applications"
publish = false

[dependencies]
